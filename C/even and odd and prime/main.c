#include <stdlib.h>
#include <stdio.h>

int main()
{
    signed int num[10] = {4, 3, 6, 5, 8, 9, 2, 7, 1, 10},
               order[10],
               i;

    printf("The order of the array is: ");
    for (i = 0; i < 10; i++)
    {
        if (num[i] < num[i + 1])
        {
            order[i] =
        }
    }
    return 0;
}