#include <avr/io.h>
#include "important_macros.h"
void dioSetPinDir (char port,char pin,char dir) {
	switch (port) {
		case 'A':
		case 'a':
			if (dir == 1) {
				SET_BIT(DDRA, pin);
			} else {
				CLR_BIT(DDRA, pin);
			}
			break;

		case 'B':
		case 'b':
			if (dir == 1) {
				SET_BIT(DDRB, pin);
			} else {
				CLR_BIT(DDRB, pin);
			}
			break;

		case 'C':
		case 'c':
			if (dir == 1) {
				DDRC |= (1 << pin);
			} else {
				DDRC &= ~(1 << pin);
			}
			break;

		case 'D':
		case 'd':
			if (dir == 1) {
				DDRD |= (1 << pin);
			} else {
				DDRD &= ~(1 << pin);
			}
			break;
	}
}
