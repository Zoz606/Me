#define F_CPU 8000000UL
#include <util/delay.h>
#include "BUTTON.h"
#include "LED.h"

int main(void) {
	BUTTON_vInit('A', 2);
	LED_vInit('D', 2);
	unsigned char x;
	while(1) {
		x = BUTTON_vRead('A', 2);
		if (== 1) {
			LED_vOn('D', 2);
		} else {
			LED_vOff('D', 2);
		}

	}
}
