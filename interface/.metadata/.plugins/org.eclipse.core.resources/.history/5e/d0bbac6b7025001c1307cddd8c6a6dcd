#define F_CPU 8000000UL
#include <util/delay.h>
#include "DIO.h"

int main(void) {
	DIO_vSetPortDir('a', 0xff);
	while(1) {
		DIO_vWritePort('a', 0xff);
		_delay_ms(1000);
		DIO_vWritePort('a', 0x0f);
		_delay_ms(1000);
	}
}
